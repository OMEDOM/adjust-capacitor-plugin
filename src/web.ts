// import { WebPlugin } from '@capacitor/core';

// import type { AdjustSDKPlugin } from './definitions';

// export class AdjustSDKWeb extends WebPlugin implements AdjustSDKPlugin {
//   async echo(options: { value: string }): Promise<{ value: string }> {
//     console.log('ECHO', options);
//     return options;
//   }

//   async initSDK(options: { appToken: string; environment?: AdjustEnvironment; logLevel?: AdjustLogLevel }): Promise<void> {
//     console.log('INIT SDK', options);
//   }
// }

// 暂不实现 web 端
